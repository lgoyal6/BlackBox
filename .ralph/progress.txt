## BlackBox — Progress Log

Voice-native flight recorder for EMS crews. MongoDB .Local Build Fest, Pier 48 SF, 2026-08-13.
Read `.ralph/overview.md` then `.ralph/contracts.md` before starting any phase.

---

## Execution Model

PHASE-01 is the only prerequisite for phases 02–15. The moment `npm run typecheck` passes in
PHASE-01, those fourteen phases can start simultaneously. PHASE-16 is serial after that wave:
flip every port from fake to real and run the smoke path. Own only your phase's files and set
the `*_MODE` env vars of ports you consume to `fake` while building.

---

## Phase Status Overview

- PHASE-01 (Contracts, Scaffold, Ports, and Fakes): COMPLETE
- PHASE-02 (Collections, Validators, and Vector Indexes): IMPLEMENTED — 3/4 `vs_*` READY on M0; `vs_postmortems` blocked until Flex
- PHASE-03 (Embeddings Provider): COMPLETE
- PHASE-04 (NYC Dispatch Ingestion and the Pitch Number): COMPLETE
- PHASE-05 (NASEMSO Runbook Corpus): COMPLETE — 183 real Voyage docs on Atlas; `vs_runbooks` READY
- PHASE-06 (Seeded Memory): COMPLETE — 40 seeded + 3 curated on Atlas; decisions=0
- PHASE-07 (Three-Collection Fan-Out Retrieval): COMPLETE. `npm run verify:retrieval` runs against the
  live cluster and correctly stops at the index-readiness gate: `vs_postmortems` does not exist yet
  (decisions/remediations/runbooks are READY at 1024 dims). This is the M0-tier 3-search-index cap
  Codex flagged — not a code bug. Relevance probes have not run past that gate.
- PHASE-08 (LangGraph, MongoDB Checkpointer, Readback Interrupt): COMPLETE and PASSING against the live
  cluster — `npm run drill` and `npm run graph:local -- --auto-confirm --print-state` both exit 0
  (checkpointCount 43 and climbing). Two real bugs found and fixed by live runs, not by typecheck or
  fake-port testing: see Issues Found & Fixed below.
- PHASE-09 (Decision Capture, Postmortem, ePCR Draft): COMPLETE. `generateAndWrite`/`draftPcr` exercised
  live via PHASE-08's `postmortem` node reaching a real Atlas write path; one bug found and fixed (see
  below). Direct `recordDecision`/`generateAndWrite` unit exercise from the phase's own verification
  script has not been re-run live this session (the graph-driven path exercises the same code).
- PHASE-10 (Event Bus and SSE Stream): COMPLETE
- PHASE-11 (Tool, Demo, and State API Routes): COMPLETE
- PHASE-12 (Change Stream Worker): COMPLETE
- PHASE-13 (ElevenLabs Voice Layer): COMPLETE
- PHASE-14 (Judge-Facing Dashboard): PENDING
- PHASE-15 (Demo Orchestration and Rehearsal): PENDING
- PHASE-16 (Fake-to-Real Cutover and End-to-End Smoke): PENDING  ← serial, after 02–15

Workstreams (see `.ralph/workstreams.md`):
- `ws/data` — PHASE-01–06
- `ws/graph` — PHASE-07–09
- `ws/runtime` — PHASE-10–13
- `ws/demo` — PHASE-14–16

---

## Pre-Build Findings (already done — do not repeat this work)

**Date:** 2026-08-13
**By:** planning session

**Verified live against the Socrata API** (`76xm-jjuj`, no auth required):
- 29,978,154 total NYC EMS incidents on record
- 845,887 of 5,653,498 incidents since 2023 had the call type change after arrival = **15.0%**, the headline number
- 2,750,007 of 29,978,154 all time = 9.2%
- 400,548 undertriaged since 2023 (severity upgraded after arrival) = 7.1%
- 237,210 incidents reopened = 0.79%
- Top transitions since 2023 include `UNC`→`ARREST` (14,987) and `SICK`→`CARD` (15,966), which became the demo pair
- Severity code direction resolved empirically: mean response time rises monotonically from code 1 (309 s) to code 8 (2837 s), so lower code = more severe. Codes 0 and 9 are noise.
- JSON API keys are lowercase snake_case and the identifier is `incident_id`, not `CAD_INCIDENT_ID`

**Verified source availability:**
- `nasemso.org` PDF returns 403 to non-browser clients; the `ems.utah.gov` mirror returns 200 with 5,040,475 bytes
- Dataset code-description attachment located at asset id `1f3c87df-ffa3-4bda-a63c-45aeac003a26`

**Verified package versions** (all current on the registry): next 16.3.0, react 19.2.8, mongodb 7.5.0,
@langchain/langgraph 1.4.9, @langchain/langgraph-checkpoint-mongodb 1.4.0, @elevenlabs/react 1.12.0,
@elevenlabs/elevenlabs-js 2.63.0, unpdf 1.8.1, voyageai 0.4.0, zod 4.4.3, tsx 4.23.12.

**Open items carried into the build:** tune `SIGNATURE_MATCH_FLOOR`; confirm `@langchain/langgraph` v1
export names against installed types; decide on the Twilio outbound upgrade after the browser
transport works.

---

## Iteration Log

## Phase 03 — Embeddings Provider — COMPLETE
**Date:** 2026-08-13
**Tasks:** US-006
**Files Created:** `src/lib/embeddings/retry.ts`, `src/lib/embeddings/batch.ts`, `src/lib/embeddings/voyage.ts`, `src/lib/embeddings/openai.ts`, `src/lib/embeddings/cache.ts`, `src/lib/embeddings/index.ts`
**Files Modified:** `.ralph/agents.md` (Voyage→OpenAI fallback procedure)
**Ports Implemented:** EmbeddingsPort at src/lib/embeddings/index.ts
**Fakes Used During Build:** none consumed — this phase is a leaf. Verified with a stub provider (no VOYAGE_API_KEY, no network).
**Key Decisions:**
- All batching/dedup/cache/dim/retry logic lives in `embedWithProvider(provider, ...)` so acceptance is checkable with a synchronous stub and zero keys.
- Provider selection is env-only. No Voyage→OpenAI failover (would change 1024→1536 mid-run and empty `$vectorSearch`).
- Cache writes are `bulkWrite` upserts; Mongo failures degrade to a miss with one warning.
**Issues Found & Fixed:**
- None — typecheck and stub verification passed on first run. Live Voyage and live `_embed_cache` checks were skipped (no `.env` / `VOYAGE_API_KEY` / `MONGODB_URI` in this environment).
**Contract Changes:** None
**Verification Results:**
- `npm run typecheck` — zero errors
- Stub: 300 texts, order preserved, dim 1024, â‰¤3 batches, duplicates embedded once, `embed([])` → `[]`
- `planBatches` covers `[0..299]` in order, max 128, oversized text is a batch of one, token budget splits
- Wrong-length and short provider responses throw
- `isRetryable` / `withRetry`: 429 → 4 attempts, 400 → 1 attempt
- `cacheKey` differs by `inputType` and model
- Registry with `EMBEDDINGS_MODE=real` resolves `{ provider: "voyage", model: "voyage-3-large", dim: 1024 }`
- Mismatched `EMBEDDING_MODEL`/`EMBEDDING_DIM` throws at import
**Next:** PHASE-05 and PHASE-06 can set `EMBEDDINGS_MODE=real` once a Voyage key and Atlas URI are present. Until then, keep `EMBEDDINGS_MODE=fake`.

_Append one entry per completed phase, using this shape:_

```
## Phase NN — <Title> — <COMPLETE|BLOCKED>
**Date:** YYYY-MM-DD
**Tasks:** US-NNN[, US-NNN]
**Files Created:** `path`[, `path`]
**Files Modified:** `path` [or "None"]
**Ports Implemented:** <PortName at src/lib/<name>/index.ts> [or "None"]
**Fakes Used During Build:** <which *_MODE were set to fake>
**Key Decisions:**
- <decision made during implementation and why>
**Issues Found & Fixed:**
- <what went wrong, how it was fixed>   [or "None — passed all checks on first attempt"]
**Contract Changes:** <what changed in contracts.md, or "None">
**Verification Results:**
- <summary of typecheck/build/script results>
**Next:** <what this unblocks>
```

_Anything under Issues Found & Fixed that another phase could hit belongs in `agents.md` under
Known Gotchas as well. That file is the only way parallel agents learn from each other._

## Phase 05 — NASEMSO Runbook Corpus — COMPLETE
**Date:** 2026-08-13
**Tasks:** US-009
**Files Created:** `src/lib/ingest/runbooks.ts`, `scripts/ingest-runbooks.ts`
**Files Modified:** `.ralph/prd.json`, `.ralph/progress.txt`, `.ralph/agents.md`, `.ralph/specs/phase-05-runbook-corpus.md`
**Ports Implemented:** None
**Fakes Used During Build:** EMBEDDINGS_MODE intended `fake` (dry-run never calls the port)
**Key Decisions:**
- Utah mirror only; cache PDF at exactly 5,040,475 bytes and pages at `data/nasemso-pages.json`
- Title detection skips the Aliases block; chapter comes from `Chapter Rev. March 2022` headers
- `sectionPath` is `[chapter, title]` on every chunk so the spoken breadcrumb stays a guideline, not a fragment
- Filter matched 22 of 69 sections (no fallback); Field Triage appendix kept
**Issues Found & Fixed:**
- First-pass titles were alias lists and TOC hits; fixed by requiring Aliases-then-title
- `NASEMSO` boilerplate was winning chapter attribution; now only the `Rev.` header counts
- unpdf leaves `7.` on its own line (often at a page break); merge with the next line
**Contract Changes:** None
**Verification Results:**
- `npm run typecheck` passes
- `npx tsx scripts/ingest-runbooks.ts --dry-run` twice: 183 chunks both times, PDF/pages cache reused
- Unfiltered 506 chunks (sanity gate 50–3000); filtered 183; min 49 / median 1358 / max 1597; 0 over budget; 100% path-length-2; 100% clean openings by ingest regex
- Field Triage kept; no reference-path leak; `assertChunkCountSane` throws on 12 and 5000
- Atlas write not run in the first pass (no URI then). Live real-embed recorded in the addendum below.
**Next:** PHASE-07 can judge runbook retrieval against the live corpus. Postmortem `$vectorSearch` still needs Flex for `vs_postmortems`.

## Phase 05 — live Atlas real-embed — COMPLETE
**Date:** 2026-08-13
**Tasks:** US-009
**Files Created:** None
**Files Modified:** `.ralph/progress.txt`, `.ralph/agents.md`, `.ralph/specs/phase-05-runbook-corpus.md`, `README.md`
**Ports Implemented:** None
**Fakes Used During Build:** `EMBEDDINGS_MODE=real` only (do not flip the rest until PHASE-16)
**Key Decisions:**
- Wrote the corpus with real Voyage `document` embeddings on the first URI+key day, skipping a fake write that PHASE-07 would have to throw away.
- Did not take M0 Plan B. `vs_runbooks` is READY; `vs_postmortems` waits on Flex.
**Issues Found & Fixed:**
- `npm run indexes` failed on `collMod` for existing `decisions` (already in agents.md) and on the fourth vector index (M0 cap). Three indexes still reached READY: `vs_decisions`, `vs_remediations`, `vs_runbooks`.
- Unpaid Voyage is 3 RPM / 10K TPM. `WRITE_BATCH=200` sent 183 texts in one embed call, 429'd after `deleteMany`, and left `runbooks` empty of `NASEMSO-2022-v3`. Recovered with 8-text batches and a 22s gap. Recorded in `agents.md`.
**Contract Changes:** None
**Verification Results:**
- 183 documents, `source=NASEMSO-2022-v3`, `embedding.length=1024`, empty `embeddedText=0`, duplicate `(sectionTitle, chunkIndex)=0`
- `vs_runbooks` status READY, queryable, 1024 dimensions
**Next:** PHASE-07 can retrieve runbooks now. Do not re-ingest unless the chunker changes. Add a Voyage payment method before any other large embed; upgrade Atlas to Flex before seeding needs `vs_postmortems`.

---

## Phase 02 — Collections, Validators, and Vector Indexes — BLOCKED (code complete)
**Date:** 2026-08-13
**Tasks:** US-004, US-005
**Files Created:** `src/lib/db/validators.ts`, `src/lib/db/indexes.ts`, `scripts/create-indexes.ts`
**Files Modified:** `.ralph/agents.md`, `.ralph/progress.txt` (tracking only)
**Ports Implemented:** None
**Fakes Used During Build:** all `*_MODE=fake` (script itself talks only to Atlas)
**Key Decisions:**
- Validator requires `incidentId`, `actionChosen`, `rationale`, `t`; no `additionalProperties: false`
- Vector filter paths match each document type; runbooks has only `sectionTitle`
- Mismatched `numDimensions` drops and recreates; `--drop-vector` waits until Atlas finishes deleting
**Issues Found & Fixed:**
- No `.env.local` / `MONGODB_URI` in this workspace, so `npm run indexes` cannot talk to Atlas. Script prints `numDimensions = 1024` then exits 1 with a clear URI error. Atlas MCP org AI access is also disabled, so the cluster cannot be provisioned from here.
**Contract Changes:** None
**Verification Results:**
- `npm run typecheck` passes
- Offline spec check: 8 managed collections, 4 vector specs (`vs_decisions`, `vs_remediations`, `vs_runbooks`, `vs_postmortems`) at `numDimensions=1024` with the exact filter paths
- Live `npm run indexes`, validator insert tests, and `$vectorSearch` smoke: not run — blocked on URI
**Next:** Add `MONGODB_URI` (Flex or higher) to `.env.local`, then `npm run indexes` and wait until all four `vs_*` are READY. That unblocks PHASE-03/05/06/07 vector writes. **Handoff: indexes will be built at numDimensions=1024.**

## Phase 01 — Contracts, Scaffold, Ports, and Fakes — COMPLETE
**Date:** 2026-08-13
**Tasks:** US-002, US-003 (pass); US-001 code complete, live Atlas check blocked on `MONGODB_URI`
**Files Created:** `package.json`, `tsconfig.json`, `next.config.ts`, `.env.example`, `.gitignore`, `README.md`, `app/layout.tsx`, `app/page.tsx`, `app/globals.css`, `src/lib/contracts/**`, `src/lib/ports.ts`, `src/lib/registry.ts`, `src/lib/env.ts`, `src/lib/db/client.ts`, `src/lib/llm.ts`, `src/lib/fakes/**`, `src/lib/real-ports.d.ts`, `scripts/check-atlas.ts`, `fixtures/{incidents,hits,runbook-chunks,postmortems,event-stream,utterances}.json`
**Files Modified:** `.ralph/prd.json`, `.ralph/progress.txt`, `.ralph/agents.md`, `.ralph/specs/phase-01-contracts-and-scaffold.md`
**Ports Implemented:** LlmPort at `src/lib/llm.ts` (real) plus seven fakes
**Fakes Used During Build:** `EMBEDDINGS_MODE=fake RETRIEVAL_MODE=fake` for registry checks; real embeddings module already existed from PHASE-03
**Key Decisions:**
- App Router at repo-root `app/` with `@/*` → `src/*` so later phases own `app/page.tsx` without a `src/app` split.
- Registry uses static `import("@/lib/<port>")` strings and `src/lib/real-ports.d.ts` so typecheck passes before those modules exist.
- Did not take M0 Plan B. Four-collection contract stands.
**Issues Found & Fixed:**
- Atlas MCP org AI access is disabled, so cluster tier could not be inspected. Recorded in agents.md. `npm run check` exits 1 with empty URI and with a corrupted URI (`ECONNREFUSED`).
- Next.js `include: **/*.ts` typechecked stray `tmp-*.ts` verify scripts. Excluded them in tsconfig.
**Contract Changes:** None
**Verification Results:**
- `npm run typecheck` — zero errors
- `npm run build` — succeeded
- `npm run dev` — HTTP 200 at `/` with BlackBox copy
- Fake embeddings: dim 1024, deterministic, cosine 0.048 between distinct strings
- Registry `*_MODE=fake` returns fakes; missing real retrieval logs `FAKE PORT`
- `assertEmbeddingConfig()` throws on voyage-3-large / 1536
- Fixtures: incidents 6, hits 12, runbook-chunks 4, postmortems 6, event-stream 40, utterances 8
**Next:** Copy `.env.example` to `.env.local`, fill a Flex-or-higher `MONGODB_URI`, run `npm run check`. Phases 02–15 are unblocked.

## Phase 06 — Seeded Memory — COMPLETE
**Date:** 2026-08-13
**Tasks:** US-010, US-011
**Files Created:** `src/lib/memory/seed.ts`, `scripts/seed-memory.ts`, `fixtures/curated-postmortems.json`
**Files Modified:** `.ralph/specs/phase-06-seeded-memory.md`, `.ralph/prd.json`, `.ralph/agents.md`, `.ralph/progress.txt`
**Ports Implemented:** None
**Fakes Used During Build:** `EMBEDDINGS_MODE=fake`, `LLM_MODE=fake` (also verified `--templated` with `LLM_MODE=real` and no `OPENAI_API_KEY`)
**Key Decisions:**
- `--templated` is the default; `--llm` is opt-in. Fake LLM narratives fail the 60–110 word band, so the default path never calls `LlmPort`.
- Family medians are cached after one aggregation (`$median` on MongoDB 7+, `$push` fallback). Fixture runs compute medians in process and never hit Atlas.
- Curated entries bind to real incident ids with derived `costMinutes`; diversion text is synthetic and labeled `origin: "curated"`. Empty `incidents` skips curated rather than blocking the bulk seed.
**Issues Found & Fixed:**
- PowerShell `npm run seed -- --target=20` is eaten as npm config; `npx tsx` and `cmd /c` pass flags through. Noted in `agents.md`.
- Voyage 429s on the live embed batch retried and succeeded (`EMBEDDINGS_MODE=real`).
- Curated B3/M2 selects were not in the 2024 slice; bound to other dispatch areas via same call-type codes and printed the bound `incidentId`.
**Contract Changes:** None
**Verification Results:**
- `npm run typecheck` passes
- Fixture dry-run: `--target=20 --templated --from-fixtures --dry-run` exits 0; 4 selected; word band 91–103; dose regex clean; `decisionsCount` 0
- Live Atlas (MongoDB 8.0.29): `npm run ingest:incidents` → 178 historical docs; then `EMBEDDINGS_MODE=real LLM_MODE=fake npx tsx scripts/seed-memory.ts --templated`
- Seed report: selected 40, postmortemsWritten 40, remediationsWritten 40, curatedWritten 3, llmFailures 0, decisionsCount 0 OK
- Transition histogram: `UNC->ARREST` 15, `SICK->CARD` 15, other divergent 10. All 43 remediations `failure` (selection prefers undertriage).
**Next:** PHASE-07 can tune `SIGNATURE_MATCH_FLOOR` against this exact corpus (43 postmortems: 40 seeded + 3 curated). Do not re-seed unless the ingest slice changes; `origin: "seeded"` deletes are idempotent.

## Phase 04 — NYC Dispatch Ingestion and the Pitch Number — COMPLETE
**Date:** 2026-08-13
**Tasks:** US-007, US-008
**Files Created:** `src/lib/ingest/nyc.ts`, `scripts/ingest-incidents.ts`, `scripts/compute-pitch-number.ts`
**Files Modified:** `.ralph/agents.md`, `.ralph/prd.json`, `.ralph/progress.txt`, `.ralph/specs/phase-04-nyc-ingestion.md`
**Ports Implemented:** None
**Fakes Used During Build:** `EMBEDDINGS_MODE=fake RETRIEVAL_MODE=fake MEMORY_MODE=fake LLM_MODE=fake EVENTS_MODE=fake GRAPH_MODE=fake VOICE_MODE=fake`
**Key Decisions:**
- `toDate` appends `Z` so naive Socrata timestamps store as UTC wall-clock; `ref` is timezone-stable.
- Slice fetches are sequential with up to three timeout/5xx retries (120s each). Parallel `$limit` requests timed out here.
- Pitch cache is the point of `npm run pitch`: default reads `data/pitch-numbers.json`; `--refresh` refetches; `--offline` fails closed if the cache is missing.
- Reclass priors are Socrata `$group` counts for `UNC`/`SICK` from 2023 onward, including night hours via `date_extract_hh`. Never a literal copied from `reference.png`.
**Issues Found & Fixed:**
- First full ingest: four parallel Socrata slice requests hit a 45s abort. Sequential 90s fetches + one retry completed in ~35s. Atlas ingest later hit a 90s abort; timeout is now 120s with three attempts.
- `verifyCodeLabels` reported 11 unlabeled codes on divergent/control only (`PEDSTR`, `ALTMEN`, `ANAPH`, `BURNMI`, `SICMIN`, `STATEP`, `STNDBM`, `STNDBY`). Demo path (`UNC`/`SICK`) is fully labeled. Recorded in `agents.md`; did not edit PHASE-01 `CODE_LABELS`.
- A leftover PowerShell `MONGODB_URI` pointing at local Docker shadowed `.env.local` until it was unset.
**Contract Changes:** None
**Verification Results:**
- `npm run typecheck` — zero errors
- Transform: `toNum("")` is null; severity 9 drops; `2024-03-05T01:12:00.000` → `ref` `240305-5942`; `synthesizeUnit` stable and `/^\d{1,2}[A-Z]$/`
- Fixture-derived rows quarantine finals under `_groundTruth`; `incidentTotalSeconds` left unset
- `--slice=arrest --dry-run`: 40 fetched, 40 transformed, 0 drops, unlabeled none
- `npm run ingest:incidents` against local Docker Mongo (no PHASE-02 indexes): 178 historical docs, 40 UNC→ARREST, 40 SICK→CARD, 20 control, 1 severity drop, 1 cross-slice duplicate, leak=0, BSON dates, only `_id_` index
- Second ingest: count 178 unchanged, upserted 0, modified 178, createdAt untouched
- Live Atlas (MongoDB 8.0.29, replica set `atlas-8o6fie-shard-0`): same 178 historical / 40 UNC→ARREST / 40 SICK→CARD / 0 live after `npm run ingest:incidents` with `.env.local` URI
- `npm run pitch -- --refresh`: all ten metrics match expected, **zero drift**, `divergent_2023_pct` = 15.0; 130 priors, UNC+SICK, nightOnly true, no mockup 18%
- Cached `npm run pitch` and `--offline` make zero network requests; `--offline` without cache exits 1
**Next:** PHASE-07 serves `data/reclass-priors.json` (SICK→CARD 2.6% population / 2.9% in B3 — not the mockup 18%). PHASE-15: the slide number is still **15.0%**. Preflight saw 3 vector indexes (M0 cap); confirm Flex before a fourth `vs_*`.

---

## Phase 10 — Event Bus and SSE Stream — COMPLETE
**Date:** 2026-08-13
**Tasks:** US-019, US-020
**Files Created:** `src/lib/events/seq.ts`, `src/lib/events/index.ts`, `src/lib/events/watch.ts`, `app/api/events/route.ts`
**Files Modified:** None
**Ports Implemented:** EventsPort at `src/lib/events/index.ts`
**Fakes Used During Build:** EMBEDDINGS/RETRIEVAL/MEMORY/LLM/GRAPH/VOICE=fake, EVENTS_MODE=real
**Key Decisions:**
- `nextSeq` is a single `$inc` `findOneAndUpdate` on `_watch_state`, namespaced `seq:*`. Verified gap-free and duplicate-free under 200 concurrent emits.
- SSE route opens the change stream before the replay `find()` and dedupes by `_id`, so nothing is lost in the snapshot/subscribe window.
- Replay boundary is SSE comments (`: replay <n>` / `: live`), not a synthetic event kind, because the §8 union is closed.
- `watchEvents` waits 250 ms for an immediate stream error so a non-replica-set target rejects rather than hanging; the route then degrades to `SSE POLL FALLBACK`.
**Issues Found & Fixed:**
- `import { recent } from "@/lib/events"` does not compile: that specifier is the ambient declaration in `real-ports.d.ts`, which exports only the default. Import `@/lib/events/index` for named exports. Logged in `agents.md`.
**Contract Changes:** None
**Verification Results:**
- typecheck clean, `npm run build` succeeds.
- 200 concurrent emits -> 200 rows, 200 unique seq, min 1 max 200; all `_id` are strings.
- `emit` resolves and logs `EVENT EMIT FAILED` with an unreachable `MONGODB_URI`, exit 0.
- 40 fixture events replayed as `: replay 40` + 40 `data:` frames, all parsing to a §8 `kind`; `: live` marker; `: ping` within 15 s; a cross-process `tsx` insert appeared as frame 41 within 2 s.
- All four response headers exact. `?incidentId=` (empty) returns 200 and warns.
- 5 connect/disconnect cycles -> last `[events] change stream closed (open=0)`.
**Next:** PHASE-14 renders these frames; PHASE-11 and PHASE-12 can call `emit` on latency-budgeted paths.

## Phase 11 — Tool, Demo, and State API Routes — COMPLETE
**Date:** 2026-08-13
**Tasks:** US-021, US-022
**Files Created:** `app/api/tools/[tool]/route.ts`, `app/api/tools/_lib/{auth,readback,counts,decision-write,handlers}.ts`, `app/api/demo/{fire,close,reset}/route.ts`, `app/api/demo/_lib/clone.ts`, `app/api/state/[incidentId]/route.ts`, `app/api/counters/route.ts`
**Files Modified:** None
**Ports Implemented:** None
**Fakes Used During Build:** GRAPH/RETRIEVAL/MEMORY/LLM/EMBEDDINGS/VOICE=fake, EVENTS_MODE=real
**Key Decisions:**
- Every `decisions` and `postmortems` write goes through `MemoryPort`; nothing here inserts or embeds. `_lib/counts.ts` is read-only and only fills `write.payload.count`.
- Added `_lib/counts.ts` beyond the spec's file table (inside the owned tree) so both emit sites share one absolute-count helper.
- `clone.ts` builds the live document field by field rather than spreading the source, so `_groundTruth` cannot carry over.
- The caller-supplied `incidentId` on `fire` selects a historical source row when one matches, and otherwise becomes the new live id — the reading that honours both sentences in the spec.
**Issues Found & Fixed:**
- The spec's older readback template (`Confirming ... Say confirm or correct me.`) conflicted with PHASE-13's pinned assertion. Resolved to `Confirm: {dose} of {drug}, {route}. Say confirm.`, which the refreshed spec now also states.
**Contract Changes:** None — the §10 Zod schemas already exist as `RecallMemoryReq` etc. in `src/lib/contracts/api.ts`.
**Verification Results:**
- typecheck clean, build succeeds; all nine route files export `runtime = "nodejs"`; both dynamic routes await `params`.
- 401 on missing and wrong secret, 404 unknown tool, 400 invalid JSON, 400 on `source: "nurse"`.
- `propose_readback` 52 ms, returns `Confirm: 1 milligram of epinephrine, IV. Say confirm.`; `recall_memory` 22 ms; `get_protocol` 4 ms, prefixed `From NASEMSO,`; summary 14 words, every `hit.spoken` <= 40 words.
- `record_decision` acked in 84 ms; the reason-bearing utterance produced exactly one `decision` event, the no-reason utterance produced none and logged `DECISION WRITE SKIPPED: empty rationale`.
- `log_timeline` emitted `write` with `collection: "timeline"` and counts 1 then 2.
- `close_call` returned a `postmortemId` and non-empty `pcrPreview` in 16 ms, and emitted `pcr` + `status` + `write`.
- `fire` arrest and cardiac both cloned with no `_groundTruth`; `confirm_readback` moved the fake graph off `readback_gate` (`next: ["readback_gate"]` -> `[]`).
- `reset` deleted 2 decisions, 1 live postmortem, 1 live remediation, 56 events, 1 checkpoint, 3 live incidents, and left 31 seeded/curated postmortems, 30 runbooks, 1 seeded remediation, 1 historical incident and all of `_watch_state` intact.
**Next:** PHASE-13's agent points its seven tools here; PHASE-16's smoke drives this path.

## Phase 12 — Change Stream Worker — COMPLETE
**Date:** 2026-08-13
**Tasks:** US-023
**Files Created:** `worker/index.ts`, `worker/state.ts`, `worker/trigger.ts`, `worker/watch.ts`, `worker/poll.ts`
**Files Modified:** None
**Ports Implemented:** None
**Fakes Used During Build:** GRAPH=fake and the rest fake; EVENTS_MODE=real for assertions
**Key Decisions:**
- One `getDb().watch()` across `decisions`/`postmortems`/`remediations` rather than three cursors, so SIGINT closes one extra cursor.
- `write.count` is `countDocuments({})` on `change.ns.coll`, never `1`.
- `requireCollection` uses `listCollections` before watching, so a missing namespace exits 1 instead of hanging.
**Issues Found & Fixed:**
- The first resume drill looked like a failure; the worker was still alive because the SIGINT had gone to the `npx` wrapper rather than the node process. Re-run against the real PID, the drill passes.
**Contract Changes:** None
**Verification Results:**
- First log line `[worker] trigger=changestream`; live insert -> `[worker] start <id>`; `isLive: false` insert -> no start.
- Two `decisions` inserts produced `write` events with counts 1 then 2 (absolute, not `1`).
- `watch:incidents` and `watch:writes` resume tokens persisted alongside PHASE-10's `seq:*` docs.
- Insert made while the worker was stopped was processed on restart via `resumeAfter`.
- `TRIGGER_MODE=poll` fired 1 s after the insert; `poll:incidents` watermark persisted.
- Missing `incidents` collection logs `MISSING COLLECTION incidents` and exits 1.
- One SIGINT closes both cursors and exits.
**Next:** PHASE-11's `fire` inserts the live incident this watches; PHASE-08's real `GraphPort.start` replaces the fake.

## Phase 13 — ElevenLabs Voice Layer — COMPLETE (browser transport not yet exercised)
**Date:** 2026-08-13
**Tasks:** US-024, US-025, US-026
**Files Created:** `src/lib/voice/{prompt,tools,agent-config,index}.ts`, `app/api/voice/conversation-token/route.ts`, `app/api/voice/signed-url/route.ts`, `app/voice/page.tsx`, `scripts/setup-agent.ts`
**Files Modified:** None
**Ports Implemented:** VoicePort at `src/lib/voice/index.ts` (all three methods)
**Fakes Used During Build:** LLM/EVENTS/EMBEDDINGS/RETRIEVAL/GRAPH/MEMORY=fake, VOICE_MODE=real
**Key Decisions:**
- API surface confirmed against the installed SDK types rather than memory; every confirmed field name is in `agents.md`.
- `SERVER_TOOLS[].timeoutMs` keeps the spec's 2–3 s budgets and `buildAgentConfig` clamps to the platform's 5 s minimum for `responseTimeoutSecs`.
- `speak` records rather than pushes: 2.63.0 exposes no server-side message injection. The brief is retrieved by a tool, so nothing depends on server-initiated speech.
- `app/voice/page.tsx` wraps the console in `ConversationProvider` (required by `useConversation`) and passes only `conversationToken` + `connectionType: "webrtc"`.
**Issues Found & Fixed:**
- `extractRationale` returns `rationale: null` for all 8 fixtures under `LLM_MODE=fake`; the fake returns one canned paraphrase that the substring guard correctly rejects. See `agents.md` — this criterion needs `LLM_MODE=real`.
**Contract Changes:** None
**Verification Results:**
- typecheck clean, build succeeds, `VoicePort` type assertion compiles with `speak`/`signedUrl`/`conversationToken`.
- `VOICE_MODE=real` -> `voice()` returns this module, no `FAKE PORT` warning; same for `EVENTS_MODE=real`.
- `--print-prompt` exits 0 with no network; the scope sentence appears exactly once; all seven sections present.
- Both `composeReadback` copies return the pinned string exactly.
- `extractRationale` returns a non-empty `actionChosen` for all 8 fixtures, never emits `protocolConflict`, and returns `null` when the fake invents a reason the utterance does not contain.
- `--dry-run`: 7 tools, correct names and URLs, `X-BlackBox-Secret` on every one, `interruptionMode: "allow"`, `disableFirstMessageInterruptions: false`, `eleven_flash_v2_5`, parameter sets matching §10 exactly.
- `setup-agent.ts` exits 1 on an empty or localhost `PUBLIC_BASE_URL`; `ELEVENLABS_API_KEY` appears nowhere under `app/voice/`.
**Unverified (needs credentials and a tunnel):** live agent create/update idempotency, `conversation-token` and `signed-url` against a real key, `/voice` connect + mic level, barge-in, the `[tool]` ordering transcript check, and tunnel reachability.
**Next:** run `npm run agent:setup` once `PUBLIC_BASE_URL` and `ELEVENLABS_API_KEY` are set, then write the returned id into `.env` as both `ELEVENLABS_AGENT_ID` and `NEXT_PUBLIC_ELEVENLABS_AGENT_ID`.

## Phase 07 — Three-Collection Fan-Out Retrieval — COMPLETE
**Date:** 2026-08-13
**Tasks:** US-012, US-013
**Files Created:** `src/lib/retrieval/pipeline.ts`, `src/lib/retrieval/fuse.ts`, `src/lib/retrieval/spoken.ts`,
`src/lib/retrieval/signature.ts`, `src/lib/retrieval/failures.ts`, `src/lib/retrieval/priors.ts`,
`src/lib/retrieval/index.ts`, `scripts/verify-retrieval.ts`
**Files Modified:** None
**Ports Implemented:** RetrievalPort at src/lib/retrieval/index.ts
**Fakes Used During Build:** EMBEDDINGS_MODE=fake (only port this phase consumes)
**Key Decisions:**
- `signatureMatch`/`failureMemory` build their own pipelines directly via `pipeline.ts`/`fuse.ts` rather
  than routing through `index.ts`'s `fanOutFrom`, to avoid a circular import and because they need
  per-source filters `fanOutFrom`'s public options don't expose. See agents.md Technical Decisions.
- Added `callTypeFamily` to the `meta` projection for decisions/postmortems/remediations to support the
  undeclared-filter-path degradation path (over-fetch + TS post-filter).
**Issues Found & Fixed:**
- None — passed all checks on first attempt.
**Contract Changes:** None.
**Verification Results:**
- `npm run typecheck`: 0 errors.
- Pipeline stage order, `$meta` capture at stage 1, `numCandidates = 20×limit`: verified against a
  synthetic query vector, no cluster.
- RRF fusion arithmetic, per-source contiguous ranks, descending rrf order, 40-word spoken cap, no
  trailing ellipsis: verified against `fixtures/hits.json`, no cluster.
- Grep criteria (`_groundTruth`, `"success"`, `runbooks` in signature.ts) all pass.
- **Unverified (no MONGODB_URI in this environment):** `npm run verify:retrieval`'s index-readiness
  check and four relevance probes, live `fanOut`/`signatureMatch`/`failureMemory`/`reclassPrior` against
  a real cluster, the empty-`decisions`-collection and missing-index degradation paths.
**Next:** Unblocks PHASE-08 and PHASE-09's real (non-fake) `RETRIEVAL_MODE=real` cutover once Atlas is
reachable. Announce: `SIGNATURE_MATCH_FLOOR` stays at the contract default (0.62) — not tuned, since no
real embeddings/seeded data were available this session.

## Phase 09 — Decision Capture, Postmortem Generator, ePCR Draft — COMPLETE
**Date:** 2026-08-13
**Tasks:** US-017, US-018
**Files Created:** `src/lib/memory/decisions.ts`, `src/lib/memory/postmortem.ts`, `src/lib/memory/epcr.ts`,
`src/lib/memory/index.ts`
**Files Modified:** None (did not touch `seed.ts`, PHASE-06's file)
**Ports Implemented:** MemoryPort at src/lib/memory/index.ts
**Fakes Used During Build:** EMBEDDINGS_MODE=fake, EVENTS_MODE=fake, LLM_MODE=fake
**Key Decisions:**
- `MemoryPort`'s real shape (`src/lib/ports.ts`, landed by PHASE-01) is narrower than this phase's own
  inner functions (`recordDecision`/`generateAndWrite`/`draftPcr` take richer input, return full docs) —
  `index.ts` is an adapter: it loads the incident (`PUBLIC_INCIDENT_PROJECTION`) and its decisions to
  derive the fields the port's narrow input lacks (`displayId`, `callTypeFamily`), and narrows the
  richer return values down to the port's `Promise<string>` / `Promise<{text}>` shapes.
- `updateOutcome` (in the port, no dedicated file in the phase spec's file table) lives in `index.ts`.
- Dropped the spec's literal `const { _groundTruth: _, ...safe } = incident` line in `postmortem.ts` —
  it fails the file's own acceptance grep (`rg _groundTruth` must return nothing) because it names the
  field as a string. Read only the needed fields off `incident` directly instead; never reference the
  quarantined field at all, textually or otherwise.
**Issues Found & Fixed:**
- The `_groundTruth` grep-vs-suggested-code conflict above. Logged in agents.md so PHASE-07/08 (which
  had the same instruction) don't repeat it — they didn't, having been written after this was caught.
**Contract Changes:** None.
**Verification Results:**
- `npm run typecheck`: 0 errors.
- `assertRationale` throws `EmptyRationaleError` on empty/whitespace before any port call (verified by
  inspection — no Mongo call precedes it in the source); `embeddedTextFor` contains the rationale as a
  substring; `draftPcr` returns exactly `PCR_SECTION_ORDER`; `renderPcrText` starts with the unsigned
  header and contains no whole-word "final"/"signed"/"complete"; `whatChangedFrom` derives only from the
  live record. All verified as pure-function checks with no cluster.
- Grep criteria (`_groundTruth`, no `llm()`/`@/lib/llm` in decisions.ts) both pass.
- **Unverified (no MONGODB_URI in this environment):** actual Atlas inserts/deletes, `write`/`decision`/
  `pcr` event emission against a real events collection, the 8-second `generateAndWrite → draftPcr →
  renderPcrText` budget under real (non-fake) ports.
**Next:** Unblocks PHASE-08's `record_decision`/`postmortem` nodes (consumed via `memory()`, not
imported directly) and PHASE-11's `record_decision`/`close_call` routes.

## Phase 08 — LangGraph, MongoDB Checkpointer, and the Readback Interrupt — COMPLETE
**Date:** 2026-08-13
**Tasks:** US-014, US-015, US-016
**Files Created:** `src/lib/graph/state.ts`, `src/lib/graph/checkpointer.ts`, `src/lib/graph/wrap.ts`,
`src/lib/graph/nodes.ts`, `src/lib/graph/compile.ts`, `src/lib/graph/index.ts`,
`scripts/run-graph-local.ts`, `scripts/kill-resume-drill.ts`
**Files Modified:** None
**Ports Implemented:** GraphPort at src/lib/graph/index.ts
**Fakes Used During Build:** RETRIEVAL_MODE=fake, LLM_MODE=fake, EVENTS_MODE=fake, EMBEDDINGS_MODE=fake,
VOICE_MODE=fake (GRAPH_MODE itself has no fake path for this phase's own code — the checkpointer needs a
real MongoClient object, though no live connection is required to construct one)
**Key Decisions:**
- Confirmed `@langchain/langgraph` 1.4.9 export names against the installed `.d.ts` before writing any
  node — see agents.md ws/graph gotchas for the full findings (interrupt/Command/Annotation/etc. all
  match the spec's assumptions; `MongoDBSaver.setup()` returns `Promise<Error[]>`, not void).
- `client as unknown as ConstructorParameters<typeof MongoDBSaver>[0]["client"]` cast in
  `checkpointer.ts` to bridge a real mongodb-version mismatch between this project's `mongodb@7.5.0` and
  `@langchain/langgraph-checkpoint-mongodb@1.4.0`'s nested `mongodb@6.21.0` dependency. See agents.md —
  this will bite any other phase that constructs anything from that checkpoint package.
- `readbackGate`'s `derivePending` fallback (no pending readback, no medic timeline yet) uses an empty
  `utterance` (not a placeholder sentence), so `recordDecisionNode`'s existing empty-utterance skip keeps
  decision/remediation counts identical across a kill-and-resume cycle on the default fixture — which is
  exactly what `npm run drill`'s core assertion requires. A non-empty placeholder utterance would have
  made the drill spuriously record a decision on resume and fail its own consistency check.
- `recordDecisionNode` writes the raw medic utterance as both `actionChosen` and `rationale` (no LLM
  split) rather than fabricating separate values in a shape the spec never actually locks in — see the
  new Open Items bullet in agents.md for the cross-phase question this raises for PHASE-11.
- `postmortemNode` emits its own `kind: "pcr"` event (with the ePCR draft's preview) in addition to the
  one PHASE-09's `generateAndWrite` already emits (with the narrative's preview) — both phase specs
  explicitly ask for this independently; kept both rather than second-guessing either spec.
**Issues Found & Fixed:**
- The mongodb version-mismatch type error above — fixed with the documented cast, not by touching
  `package.json` (PHASE-01-owned).
- `tsx -e "<multiline>"` silently no-ops in this shell — switched to real `.ts` files for every
  verification step beyond a one-liner.
**Contract Changes:** None.
**Verification Results:**
- `npm run typecheck`: 0 errors across the whole repo.
- `rg MemorySaver` (repo-wide, excluding node_modules/.ralph): zero hits.
- `rg _groundTruth src/lib/graph`: zero hits. No file in this phase's ownership imports
  `@/lib/memory/*` or `@/lib/retrieval/*` directly (only via `registry`).
- `triage`/`signatureMatchNode`/`brief`/`plan` exercised directly (not through the compiled graph, which
  needs a live checkpointer) with `RETRIEVAL_MODE=fake LLM_MODE=fake EVENTS_MODE=fake`: brief ≤55 words,
  contains "new signature, no prior history" when unmatched, no raw dispatch code after the code-filter
  post-pass; `plan.excludedPaths` non-empty for a SICK/general fixture incident against
  `fixtures/hits.json` (the phase's own stated parallel-safe criterion); no dose pattern survives the
  steps filter.
- **Unverified (no MONGODB_URI in this environment, and this phase's own scope explicitly excludes
  building a smoke harness — that's PHASE-16):** `MongoDBSaver.setup()` against a real cluster, a full
  `graph:local --auto-confirm` run through the compiled StateGraph, `npm run drill`'s actual
  kill-and-spawn-child-process cycle, `checkpoints`/`checkpoint_writes` becoming non-empty, the
  `readback_gate`/`await_input` interrupt round-trip through real LangGraph state persistence.
**Next:** Unblocks PHASE-11 (tool routes call `graph()` from the registry) and PHASE-16 (smoke calls
`npm run drill`, must not reimplement it). First person with a reachable Atlas cluster: run
`npm run drill`, `npm run graph:local -- --auto-confirm --print-state`, and `npm run verify:retrieval`
before trusting any of PHASE-07/08/09 on stage — none of the Atlas-dependent paths have executed yet.

## PHASE-07/08/09 — Live Atlas Verification Addendum — COMPLETE
**Date:** 2026-08-13 (later same session)
**Context:** Codex provisioned a live Atlas cluster and dropped `MONGODB_URI`/`MONGODB_DB` into
`.env.local`. Re-ran everything the entries above marked "unverified." Two real bugs surfaced — neither
caught by `tsc` or by testing against fakes, both only visible against the actual `.compile()`/write path:

1. **LangGraph 1.4.9 refuses a node name that collides with a state-channel name.** `IncidentState` has
   both a `brief` field and a `plan` field, and `GRAPH_NODE_ORDER` (contracts.md) uses `"brief"` and
   `"plan"` as node names — a real collision baked into the locked contract itself. `.compile()` threw
   `"brief is already being used as a state attribute... cannot also be used as a node name"` the moment
   `GRAPH_MODE=real` actually built the graph. **Fix (`src/lib/graph/compile.ts`):** register those two
   nodes under distinct internal keys (`brief_step`, `plan_step`) in `addNode`/`addEdge`/
   `addConditionalEdges`; `wrapNode`'s `name` argument (and therefore `nodeTrail`/emitted `node` events)
   is untouched and still exactly matches `GRAPH_NODE_ORDER`. Only `GraphPort.state().next` would ever
   show the renamed key, and only transiently between super-steps — never at the two nodes that actually
   call `interrupt()` (`readback_gate`, `await_input`), so no acceptance criterion is affected.
2. **`src/lib/memory/index.ts`'s `loadIncident` had no fixture fallback**, unlike
   `src/lib/graph/index.ts`'s (which the PHASE-08 spec explicitly requires for `graph:local`/`drill`
   before PHASE-04 ingestion exists). Since the `incidents` collection is empty (PHASE-04 hasn't run),
   `postmortemNode` → `memory().generateAndWrite()` threw `"memory: incident ... not found"` and killed
   an otherwise-passing `graph:local --auto-confirm` run. **Fix:** added the same
   Atlas-then-`fixtures/incidents.json` fallback to `loadIncident`, mirroring the graph module.
3. **All three CLI scripts (`run-graph-local.ts`, `kill-resume-drill.ts`, `verify-retrieval.ts`) hung
   forever on success** — a live `MongoClient` connection keeps Node's event loop open, and none of the
   three called `process.exit(0)` on their success path (only on explicit failure branches). This is why
   `kill-resume-drill.ts`'s spawned `--resume-only` child printed its result and then never exited,
   which hung the parent's `child.on("exit")` wait indefinitely. **Fix:** all three now end with
   `main().then(() => process.exit(0)).catch(...)`.

**Verification Results (live, `GRAPH_MODE=real`, all other consumed ports `*_MODE=fake`, real Atlas):**
- `npm run drill`: **PASS**, exit 0. Parked at the readback interrupt, spawned a fresh child process,
  resumed via `MongoDBSaver` in that fresh process, and the pre/post snapshot matched exactly on the
  assertions that matter: timeline grew by exactly the 2 new `execute_record`/`verify` entries, `signature`
  byte-identical, `decisions`/`remediations` counts identical (0/0 — no duplicate writes across the kill
  boundary).
- `npm run graph:local -- --auto-confirm --print-state`: **PASS**, exit 0. `nodeTrail` reaches
  `"postmortem"`, `brief` ≤55 words with no raw dispatch code, `plan.excludedPaths` non-empty,
  `checkpointCount: 43` (climbing — see the note below on why). Confirms `MongoDBSaver.setup()`,
  `postmortemNode` → `memory().generateAndWrite()` → `draftPcr()`, and the readback→await_input→postmortem
  routing all work against a real cluster.
- `npm run verify:retrieval`: **Correctly fails at its own index-readiness gate**, before issuing any
  query, exactly as specced. Printed status: `vs_decisions`, `vs_remediations`, `vs_runbooks` are all
  `READY` at 1024 dimensions; **`vs_postmortems` does not exist.** This is the M0 free-tier 3-search-index
  cap `overview.md` and Codex's Atlas handoff note both already flagged (this cluster has hit it exactly:
  3 of 4 indexes exist). Not a code bug — needs either the Flex upgrade or PHASE-02's `merge into one
  `memory` collection` fallback plan. Relevance probes past that gate have not run.
- **Non-obvious operational finding, not a bug:** `timeline`/`nodeTrail`/`retrieved`/`decisionsRecorded`
  are concat-reducer channels, and LangGraph merges a fresh `invoke(initialState, config)` call against
  the *existing* checkpoint for that `thread_id` rather than replacing it wholesale — so repeated
  `start()` calls against the same `incidentId` (e.g. re-running `graph:local`/`drill` against the
  default fixture incident during iteration) accumulate state forever instead of resetting. This is
  exactly why `POST /api/demo/reset` (PHASE-11, deletes checkpoints) exists; it isn't optional cleanup,
  it's required between demo takes on the same incident id. Worth a one-line callout in PHASE-15's rehearsal
  script.
**Next:** Someone needs to either upgrade the Atlas tier or apply PHASE-02's documented fallback (merge
`decisions`/`remediations`/`postmortems` into one `memory` collection, 2 indexes) before
`verify:retrieval`'s relevance probes or a real (non-fake) `RETRIEVAL_MODE=real` fan-out can be exercised.
Everything else in PHASE-07/08/09 is now verified against the real cluster, not just fakes.
